

object Ex {
  def main(args: Array[String]): Unit = {

    //    val v = Vector(1, 2, 3);
    //    println(v.sum)
    //    println(v.filter { x => x > 1 })
    //    val vv = v.map { x => x * 2 }
    //    println(vv)
    
    //-----------------------------------------
    
    var idxSeq=IndexedSeq(1,2,3)
    var linSeq=scala.collection.immutable.LinearSeq(1,2,3)

  }
}